# ~/.ctxrc — Context shell startup file

export EDITOR=nano
export VISUAL=nano

# common aliases
alias ll='ls -la'
alias gs='git status'
alias gp='git push'
alias gd='git diff'
alias gc='git commit'
alias ..='cd ..'
